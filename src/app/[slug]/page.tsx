import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Page",
  description: "",
};

export default function Page() {
  return (
    <div>
      <h1>Page</h1>
    </div>
  )
}